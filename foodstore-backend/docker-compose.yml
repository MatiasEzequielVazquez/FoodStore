services:
  postgres:
    image: postgres:16
    container_name: foodstore-db
    environment:
      POSTGRES_DB: foodstore
      POSTGRES_USER: foodstore
      POSTGRES_PASSWORD: foodstore
    ports:
      - "5432:5432"
    volumes:
      - foodstore-data:/var/lib/postgresql/data

volumes:
  foodstore-data: